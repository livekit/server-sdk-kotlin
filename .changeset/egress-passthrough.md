---
"server-sdk-kotlin": minor
---

Update protocol: `startEgress` accepts the new `EncodingOptionsPreset.PASSTHROUGH` to skip transcoding for a single-track `MediaSource`. Removes the unused `UpdateEgressRequest` message.
