---
"server-sdk-kotlin": minor
---

Update agent dispatch and connector clients for new proto fields: `restartPolicy` on `createDispatch`, `disconnectReason` on `disconnectWhatsAppCall`, `waitUntilAnswered` on `acceptWhatsAppCall`, and `ringingTimeout` on `WhatsAppCallOptions`
